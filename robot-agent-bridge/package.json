{
  "name": "robot-agent-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Local broker that lets an external AI agent drive the 6DOF arm through the existing web app.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
